PORT=4003
AWS_REGION=ap-east-1
SQS_QUEUE_URL=http://localhost:9324/000000000000/votes
# Only for local development against ElasticMQ; leave unset in AWS
SQS_ENDPOINT=http://localhost:9324
# Parallel long-polling loops per process (each receives up to 10 messages)
WORKER_CONCURRENCY=4
DB_HOST=localhost
DB_PORT=3306
DB_NAME=questionnaire
DB_USER=questionnaire
DB_PASSWORD=questionnaire
DB_SSL=false
DB_POOL_SIZE=5
