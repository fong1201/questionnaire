PORT=4002
DB_HOST=localhost
DB_PORT=3306
DB_NAME=questionnaire
DB_USER=questionnaire
DB_PASSWORD=questionnaire
DB_SSL=false
# Comma separated; leave empty when served behind CloudFront on the same origin
CORS_ORIGINS=
# Seconds dashboard results are cached in-process (and by browsers/CloudFront)
STATS_CACHE_SECONDS=2
DB_POOL_SIZE=5
