{
  "name": "questionnaire-monorepo",
  "private": true,
  "version": "0.1.0",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "packages/*",
    "backend/*",
    "frontend/*"
  ],
  "scripts": {
    "build": "npm run build:packages && npm run build -w @questionnaire/questionnaire-api -w @questionnaire/vote-worker -w @questionnaire/dashboard-api -w @questionnaire/questionnaire-web -w @questionnaire/dashboard-web",
    "build:packages": "npm run build -w @questionnaire/shared && npm run build -w @questionnaire/database",
    "lint": "eslint .",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "dev": "sh scripts/dev.sh",
    "dev:stop": "docker compose stop mysql elasticmq elasticmq-ui",
    "db:up": "docker compose up -d --wait mysql elasticmq elasticmq-ui",
    "db:migrate": "npm run migration:run -w @questionnaire/database",
    "dev:questionnaire-api": "npm run start:dev -w @questionnaire/questionnaire-api",
    "dev:dashboard-api": "npm run start:dev -w @questionnaire/dashboard-api",
    "dev:questionnaire-web": "npm run dev -w @questionnaire/questionnaire-web",
    "dev:dashboard-web": "npm run dev -w @questionnaire/dashboard-web",
    "dev:vote-worker": "npm run start:dev -w @questionnaire/vote-worker"
  },
  "devDependencies": {
    "@eslint/js": "^9.33.0",
    "concurrently": "^10.0.5",
    "eslint": "^9.33.0",
    "globals": "^16.3.0",
    "typescript": "~6.0.3",
    "typescript-eslint": "^8.40.0"
  }
}
